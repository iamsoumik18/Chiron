<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evolver.chiron">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GetInfo">
        <activity
            android:name="com.evolver.chiron.DashBoardUser"
            android:exported="false" />
        <activity
            android:name="com.evolver.chiron.UserSignUp"
            android:exported="false" />
        <activity
            android:name="com.evolver.chiron.ForgetPassword"
            android:exported="false" />
        <activity
            android:name="com.evolver.chiron.UserSignIn"
            android:exported="false" />
        <activity
            android:name="com.evolver.chiron.SignUpAdmin"
            android:exported="false" />
        <activity
            android:name="com.evolver.chiron.MainLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>